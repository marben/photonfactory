#include "Color.h"
using namespace PF;


/*
void Color::normalize(){
	if(red > 1.0)
		red = 1.0;
	else
		if(red < 0)
			red = 0;

	if(green > 1.0)
		green = 1.0;
	else
		if(green < 0.0)
			green = 0;

	if(blue > 1.0)
		blue = 1.0;
	else
		if(blue < 0.0)
			blue = 0.0;
}
*/

